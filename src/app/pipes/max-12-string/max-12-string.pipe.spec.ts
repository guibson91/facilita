import { Max12StringPipe } from './max-12-string.pipe';

describe('Max12StringPipe', () => {
  it('create an instance', () => {
    const pipe = new Max12StringPipe();
    expect(pipe).toBeTruthy();
  });
});
