import { PrintPermissionPipe } from './print-permission.pipe';

describe('PrintPermissionPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintPermissionPipe();
    expect(pipe).toBeTruthy();
  });
});
